# Inside the existing mesq.me HTTPS server block.
location = /globe-tsl-blocks {
    return 301 /globe-tsl-blocks/$is_args$args;
}

location ^~ /globe-tsl-blocks/ {
    root /var/www;
    index index.html;
    try_files $uri $uri/ =404;
}
